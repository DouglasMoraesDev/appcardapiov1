Testes automatizados do frontend usando React Testing Library e Jest.

Para rodar os testes:

1. Instale as dependências:
   npm install --save-dev jest @testing-library/react @testing-library/jest-dom ts-jest

2. Execute:
   npm test
