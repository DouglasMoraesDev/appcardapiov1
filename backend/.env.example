# Exemplo de variáveis de ambiente para o backend
# Copie este arquivo para `.env` e preencha os valores reais localmente.

# URL de conexão do MySQL (sem aspas) — no Railway use o value abaixo (inclui senha):
DATABASE_URL=mysql://root:SceCtAmEmNLAZbRRnrdGwjYbfKmHXDhQ@yamanote.proxy.rlwy.net:32248/railway

# Origem(s) permitidas do frontend (separe múltiplas com vírgula)
# Para dev locais: http://localhost:5173 (vite default) ou http://localhost:3000
# Para produção no Railway (frontend hospedado separadamente) use a URL pública:
FRONTEND_ORIGIN=http://localhost:5173,https://appcardapio-production.up.railway.app

# JWT secret — substitua por um valor forte em produção
JWT_SECRET=replace_with_a_strong_secret

# Porta do backend (opcional)
PORT=4000

# Observação: NÃO comite o arquivo `.env` contendo segredos no repositório.